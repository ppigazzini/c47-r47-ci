# Valgrind memcheck baseline for c47 - known upstream-owned findings.
#
# Each line is "<memcheck error kind> @ <c47 source file>:<line>" as emitted by
# scripts/test/run-valgrind.sh. Lines here are KNOWN, accepted upstream c47
# findings; the lane FAILS on any c47-owned uninitialised read, invalid access,
# or leak that is not listed here. Keep this set minimal and file the entries
# upstream.
#
# c47 is per-operation malloc-clean: definitely/indirectly lost stay zero on
# current master, and no leak is baselined at all. The one-time LCD frame buffer
# that used to sit here is allocated by src/testSuite/hal/lcd.c - the harness,
# not the product - and the lane now stops the ownership walk at that frame
# instead of charging it to its first c47 caller, so it is reported under
# "harness-owned allocations" and never gates. The leak kind is normalised to
# the loss category, not the run-varying byte/block count and loss-record index,
# so a leak site keys to a stable signature across runs.
#
# The uninitialised-read (conditional-jump) line set is TOOLCHAIN-DEPENDENT: how
# many jumps memcheck attributes inside a function, and at which lines, varies
# with the valgrind and gcc versions. This baseline therefore reflects the CI
# runner (ubuntu-latest), not a local machine - the CI toolchain flags more
# sites in real34ToDisplayString2 than a typical local valgrind. Regenerate ON
# THE RUNNER after a legitimate upstream change: run the lane with
# UPDATE_BASELINE=1 and take valgrind-found.txt from the uploaded ci-artifacts,
# rather than regenerating locally (a local set will be missing runner-only
# sites and the gate will fail).
#
# History: re-verified at d1b643e7a and 7d4308229. Regenerated at 9c9ac6549 from
# the CI runner (real34ToDisplayString2 flags display.c:557/558/564/581/584).
# Re-verified at ef694abb0: display.c and matrixEditor.c are byte-identical since
# 9c9ac6549 so those sites are unchanged; saveRestoreBackup.c changed only below
# line 193 so :193 holds; the config.c LCD-buffer leak shifted 1704 -> 1713 as
# config.c grew from the short-integer word-size fix (MR 1530). Re-verified
# unchanged at upstream master a361b6797 (config.c, saveRestoreBackup.c, display.c
# and matrixEditor.c are all untouched since ef694abb0). definitely/indirectly
# lost stay zero. Re-verified at upstream master 1624e09b3: the three
# line-keyed sites drifted with the source and were re-keyed - matrixEditor.c
# 984 -> 1031 (the fix/matrix-ij-save-restore MR and the editor-position-restore
# commit added lines above displayVectorAngle; the uninitialised read is in
# displayVectorAngle, not in the I/J shadow code those commits added),
# saveRestoreBackup.c 193 -> 194, config.c 1713 -> 1714. Same three sites, same
# error kinds; the five display.c sites are unchanged. definitely/indirectly
# lost stay zero. Re-keyed at upstream master 33328e4cc: ac3e45e8b inserted 15
# lines into display.c above every baselined site (two pure insertions at old
# 281 and old 309), so all five shifted +15 - 557/558/564/581/584 ->
# 572/573/579/596/599. The shift is derived from the source, not from a fresh
# memcheck run: each new line is byte-identical to the old one it replaces, and
# the 1-line hunk @@ -576 +591 @@ confirms the same +15. matrixEditor.c,
# saveRestoreBackup.c and config.c are untouched since 1624e09b3, so those three
# entries stand. Confirm against valgrind-found.txt on the next runner pass.
# The config.c LCD-buffer entry is GONE at upstream master 5697da162, and not
# because it was re-keyed a fifth time: 85b1636da moved the simulator display
# into a real frame buffer in src/testSuite/hal/lcd.c, and the lane now
# recognises that the harness owns that allocation. The entry had drifted
# 1704 -> 1713 -> 1714 -> 1727 across four upstream commits, failing the lane
# each time, while naming a file that never allocated the block. Ownership is
# read from the allocating frame now, so the drift cannot recur.
# Re-keyed at upstream master daada38417 from the runner's own valgrind-found.txt
# (run 32546139136), not from a local memcheck pass. saveRestoreBackup.c
# 194 -> 197: the sprintf in the hexDump ASCII column is byte-identical at both
# commits and sits three lines lower. The five display.c entries and
# matrixEditor.c:1031 are dropped rather than re-keyed - the runner found none of
# them at aef442e4d, the last green commit, and the only display.c site at
# daada38417 is 654, in the same real34ToDisplayString2. Both keys still hold at
# master 9af328a87: saveRestoreBackup.c is untouched since daada38417, and every
# display.c hunk since is past line 3000.
#
# Known upstream uninitialised-value read in the c47 real formatter.
# real34ToDisplayString2 explodes the value into a bcd digit buffer and scans it
# for the first significant digit; that scan is the flagged jump. The reaching
# path is the matrix editor's column-width pass - getRealMatrixColumnWidths and
# getComplexMatrixColumnWidths format every element - and memcheck attributes the
# value to the frames of insColComplexMatrix and delColRealMatrix, which the
# mateditscroll_cov reshape cases drive.
#
# Unlike every other entry here, this one is NOT established as harmless. The
# corpus case that drives the path asserts the scroll offset, not the rendered
# glyphs, so its pass says nothing about the digits. It is listed so the gate
# keeps reporting genuinely new sites, and it needs an upstream report and a
# verdict on whether a displayed digit can change.
#   - display.c:654   real34ToDisplayString2, via the matrix editor column widths
Conditional jump or move depends on uninitialised value(s) @ display.c:654

# Known uninitialised read in the backup hexDump serializer. doFnReset mallocs the
# global-register and free-region arrays (globalRegister, freeMemoryRegions in
# doFnReset) without memset, so their unused entries stay uninitialised; saveCalc
# then hexDumps each array whole and the dump's ASCII column reads every byte.
# Dump-only: the uninitialised bytes are written to the .cfg as hex and are
# harmless on restore (only the used entries matter). Verified on current master
# d1b643e7a. To be filed upstream (memset the arrays after malloc in doFnReset).
Conditional jump or move depends on uninitialised value(s) @ saveRestoreBackup.c:197
