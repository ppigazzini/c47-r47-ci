# Valgrind memcheck baseline for c47 - known upstream-owned findings.
#
# Each line is "<memcheck error kind> @ <c47 source file>:<line>" as emitted by
# scripts/test/run-valgrind.sh. Lines here are KNOWN, accepted upstream c47
# findings; the lane FAILS on any c47-owned uninitialised read, invalid access,
# or leak that is not listed here. Keep this set minimal and file the entries
# upstream.
#
# c47 is per-operation malloc-clean: definitely/indirectly lost stay zero on
# current master. The single possibly-lost entry is a one-time frame-buffer
# allocation held for process life (see config.c:1714 below). The leak kind is
# normalised to the loss category, not the run-varying byte/block count and
# loss-record index, so a leak site keys to a stable signature across runs.
#
# The uninitialised-read (conditional-jump) line set is TOOLCHAIN-DEPENDENT: how
# many jumps memcheck attributes inside a function, and at which lines, varies
# with the valgrind and gcc versions. This baseline therefore reflects the CI
# runner (ubuntu-latest), not a local machine - the CI toolchain flags more
# sites in real34ToDisplayString2 than a typical local valgrind. Regenerate ON
# THE RUNNER after a legitimate upstream change: run the lane with
# UPDATE_BASELINE=1 and take valgrind-found.txt from the uploaded ci-artifacts,
# rather than regenerating locally (a local set will be missing runner-only
# sites and the gate will fail).
#
# History: re-verified at d1b643e7a and 7d4308229. Regenerated at 9c9ac6549 from
# the CI runner (real34ToDisplayString2 flags display.c:557/558/564/581/584).
# Re-verified at ef694abb0: display.c and matrixEditor.c are byte-identical since
# 9c9ac6549 so those sites are unchanged; saveRestoreBackup.c changed only below
# line 193 so :193 holds; the config.c LCD-buffer leak shifted 1704 -> 1713 as
# config.c grew from the short-integer word-size fix (MR 1530). Re-verified
# unchanged at upstream master a361b6797 (config.c, saveRestoreBackup.c, display.c
# and matrixEditor.c are all untouched since ef694abb0). definitely/indirectly
# lost stay zero. Re-verified at upstream master 1624e09b3: the three
# line-keyed sites drifted with the source and were re-keyed - matrixEditor.c
# 984 -> 1031 (the fix/matrix-ij-save-restore MR and the editor-position-restore
# commit added lines above displayVectorAngle; the uninitialised read is in
# displayVectorAngle, not in the I/J shadow code those commits added),
# saveRestoreBackup.c 193 -> 194, config.c 1713 -> 1714. Same three sites, same
# error kinds; the five display.c sites are unchanged. definitely/indirectly
# lost stay zero.
#
# Known upstream uninitialised-value reads in c47 display/formatting paths.
# Display-only: every math test passes and no memory is corrupted. To be filed
# upstream.
#   - matrixEditor.c:1031         displayVectorAngle, via showRealMatrix redraw
#   - display.c:557/558/564/581/584  real34ToDisplayString2, via register-line
#                                    refresh and real/eigenvalue formatting
Conditional jump or move depends on uninitialised value(s) @ matrixEditor.c:1031
Conditional jump or move depends on uninitialised value(s) @ display.c:557
Conditional jump or move depends on uninitialised value(s) @ display.c:558
Conditional jump or move depends on uninitialised value(s) @ display.c:564
Conditional jump or move depends on uninitialised value(s) @ display.c:581
Conditional jump or move depends on uninitialised value(s) @ display.c:584

# Known uninitialised read in the backup hexDump serializer. doFnReset mallocs the
# global-register and free-region arrays (globalRegister, freeMemoryRegions in
# doFnReset) without memset, so their unused entries stay uninitialised; saveCalc
# then hexDumps each array whole and the dump's ASCII column reads every byte.
# Dump-only: the uninitialised bytes are written to the .cfg as hex and are
# harmless on restore (only the used entries matter). Verified on current master
# d1b643e7a. To be filed upstream (memset the arrays after malloc in doFnReset).
Conditional jump or move depends on uninitialised value(s) @ saveRestoreBackup.c:194

# One-time LCD frame-buffer allocation, not a per-operation leak. On the first
# reset, doFnReset (config.c:1714) calls lcd_fill_rect -> bitblt24 ->
# ensureLcdBuffer, which lazily calloc's the ~12 KB simulator frame buffer and
# holds it for the process lifetime. memcheck reports it "possibly lost"
# (reachable only through an interior pointer at exit). Benign and deterministic:
# it appears exactly once per run. lcd.c is not c47-owned, so the finding is
# attributed to the first c47 frame, doFnReset.
possibly lost @ config.c:1714
