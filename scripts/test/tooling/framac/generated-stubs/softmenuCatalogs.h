/* Parse-only scaffolding for Frama-C. NOT the real generateCatalogs output
   (which DEFINES the menu_* arrays). Empty guard: no Tier-A/B target references
   a catalog array. A real built tree's header takes precedence. */
#if !defined(SOFTMENUCATALOGS_H)
  #define SOFTMENUCATALOGS_H
#endif
