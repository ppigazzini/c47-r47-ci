# scripts/test/tooling/statesweep/baseline.txt
# The mutants statesweep.py still flags, one per line:
#   <mutant> | <property> | <where it lands> | <attribution>
# A name here is a known defect, not a passing test. A name NOT here appearing in
# the output is a regression; a name here that stops appearing is a fix, and this
# file should lose the line in the same commit - which is what happened to both
# entries this file opened with.
#
# Measured with the ASan simulator (meson -Db_sanitize=address), both seeds, 210
# mutants each, and 85 for d47sweep.py.
#
# EMPTY, and that is the point of the file. It opened with two rows - a state
# file with EQUATIONS count 32767 (freeListAlloc wrote to allocatedMemoryRegions[]
# before checking it had room) and one truncated a line into PROGRAMS
# (resizeProgramMemory(0) put beginOfProgramMemory one past the pool, where
# scanLabelsAndPrograms reads it). Both reproduced on upstream 199477075 and both
# are fixed on the c43 !1631 branch, so both rows went. A sweep that ends on
# "0 failure(s)" is the only baseline worth having; anything else is a defect
# someone decided to live with, and it should say whose decision it was.
