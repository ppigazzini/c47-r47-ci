# c43 per-test pool/GMP growth baseline.
# Cases on upstream master where a test grew the RAM pool past the high-water.
# run-testmem.sh fails on any growth case NOT listed here.
# Format: <file>|<IN parameter line>
#
# Regenerated against upstream master d1b643e7a: empty. The matrix-op per-call
# pool leaks previously listed (add/subtract dimension mismatch, cross product,
# real-matrix conjugate, singular LU, prime factorisation, eigenvalue error path)
# are fixed upstream. The toReal growth the scanner still prints is allocated in
# the tooling's own testMemClearWorkingState, not a corpus case, so it carries no
# case line and is not a baselineable growth.
